{
  "buildCommand": "pnpm --filter @workspace/pregnancy-tracker run build",
  "outputDirectory": "artifacts/pregnancy-tracker/dist/public",
  "installCommand": "pnpm install --frozen-lockfile",
  "routes": [
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
