rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Single-user app — allow full read/write from the app.
    // TODO: add auth checks (e.g. Firebase Auth UID) before public launch.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
